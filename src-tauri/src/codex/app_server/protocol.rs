//! Typed subset of the codex app-server v2 protocol we use: `initialize` /
//! `initialized`, `thread/start`, `turn/start` (skill input), `turn/interrupt`,
//! and the streaming `ServerNotification`s. Regenerate types from the installed
//! binary with `codex app-server generate-ts`. PR5.
