// Config slice wire types are generated from Rust (`src-tauri/src/config/model.rs` and
// `src-tauri/src/remote/status.rs`). Keep this facade so existing imports stay stable.
export * from "./types.generated";
